{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "fvm flutter build web --release --web-renderer=canvaskit",
    "deploy": "yarn build && yarn firebase deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^10.0.1"
  }
}
